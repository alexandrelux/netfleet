import { cardHeight, cardWidht } from "@/app/utils/const";

export const styles = {
    main: {
        class: `rounded-lg w-[${cardWidht}px] h-[${cardHeight}px] shrink-0 grow-0 flex items-center justify-center object-cover`,
    },
};
